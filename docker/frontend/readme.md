
```
npx ng build --configuration production
npm install -g @angular/cli
npx ng new app-frontend
cd app-frontend
npx run build
npx run start
npx ng build
npx ng build --configuration production
```

